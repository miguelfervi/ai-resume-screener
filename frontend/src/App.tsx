import { Button } from '@/components/ui/button'

function App() {
  return (
    <main className="flex min-h-dvh flex-col items-start gap-3 p-8">
      <h1 className="text-2xl font-semibold tracking-tight">AI Resume Screener</h1>
      <p className="text-muted-foreground">Chat UI coming next.</p>
      <Button type="button" variant="outline">
        Tailwind + shadcn ready
      </Button>
    </main>
  )
}

export default App
